// src/core/order.cpp
//
// Anchor TU for the core translation unit. Keeps build definitions stable
// even as we add non-inline helpers later.

#include "tt/core/order.hpp"

namespace tt {
// Intentionally empty.
}  // namespace tt
